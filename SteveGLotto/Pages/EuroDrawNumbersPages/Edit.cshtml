@page
@model SteveGLotto.Pages.EuroDrawNumbersPages.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>edit euro lottery draw number</h2>
<br />
<h4>euro lottery draw number</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EuroDrawNumbers.EuroNumberID" />
            <div class="form-group">
                <label asp-for="EuroDrawNumbers.EuroNumber" class="control-label"></label>
                <input asp-for="EuroDrawNumbers.EuroNumber" class="form-control" />
                <span asp-validation-for="EuroDrawNumbers.EuroNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="save" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-primary" asp-page="./Index">back to list</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
