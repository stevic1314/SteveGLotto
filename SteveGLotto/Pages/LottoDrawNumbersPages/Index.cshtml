@page
@model SteveGLotto.Pages.LottoDrawNumbersPages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>lotto numbers</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LottoDrawNumbers[0].LottoNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LottoDrawNumbers) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LottoNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.LottoNumberID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.LottoNumberID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.LottoNumberID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
